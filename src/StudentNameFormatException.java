public class StudentNameFormatException extends IllegalAccessException { // StudentNameFormatException 클래스는 프로그래머가 직접 만든 예외 처리. 즉, 사용자 정의 예외 처리 클래스.
    // 생성자
    public StudentNameFormatException(String message) {
        super(message);
    }
}
